interface:
  display_name: "Test UI"
  short_description: "Run fail-fast console UI test plans"
  default_prompt: "Use $test-ui to run the console UI tests in test/ui-test-plan.md and report the transcript."
